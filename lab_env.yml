variables:
  # TARGET_DESIGN can be "example", "alu", "reg_file", "simple_cpu", "custom_cpu"...
  TARGET_DESIGN: "example"

  # CPU_ISA must be either "mips" or "riscv32"
  CPU_ISA: "mips"

  # CPU frequency must be a decimal value
  CPU_FREQ: 100

  SIM_DUT_TYPE: "single_cycle"
